## What this changes

<!-- One or two sentences. Which app / lane / tool. -->

## How it was verified

- [ ] `npm run typecheck && npm test && npm run build` pass locally
- [ ] Run live against the app (which app + version?): 
- [ ] Panel changes: `npm run panels:sync` run; panel reloaded in the app

## Notes for reviewers

<!-- ExtendScript must stay ES3 (var, no arrow functions). No Adobe sample code copied. No keys or tokens in the diff. -->
